import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'
import { createServerClient } from '@supabase/ssr'

export async function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl

  // Redirect root path to programs page
  if (pathname === '/') {
    return NextResponse.redirect(new URL('/programs', request.url))
  }

  // TEMPORARILY DISABLE AUTH PROTECTION TO TEST LOGIN
  console.log('Middleware: Allowing access to', pathname, '(auth protection disabled)')
  return NextResponse.next()
}

export const config = {
  matcher: [
    /*
     * Match all request paths except for the ones starting with:
     * - api (API routes)
     * - _next/static (static files)
     * - _next/image (image optimization files)
     * - favicon.ico (favicon file)
     */
    '/((?!api|_next/static|_next/image|favicon.ico).*)',
  ],
}